{
  "name": "directdrive-authority-engine",
  "version": "1.0.0",
  "description": "DirectDrive Authority Engine - AI Citation Monitoring and Content Generation System",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "setup": "bash setup.sh",
    "dev": "npm run dev --workspaces",
    "dev:dashboard": "cd apps/dashboard && npm run dev",
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces",
    "test:all": "bash scripts/run-tests.sh",
    "lint": "npm run lint --workspaces"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/node": "^20.0.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}